spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/gym_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  liquibase:
    user: postgres
    password: postgres

logging:
  level:
    root: INFO
    ua.ivan.epam.gym.application: DEBUG
    org.hibernate.SQL: DEBUG